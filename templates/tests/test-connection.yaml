apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "insights-beta.fullname" . }}-test-connection"
  labels:
    {{- include "insights-beta.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "insights-beta.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never

---
k