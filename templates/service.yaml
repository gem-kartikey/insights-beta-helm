apiVersion: v1
kind: Service
metadata:
  name: {{ include "insights_backend.fullname" . }}
  labels:
    {{- include "insights_backend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "insights_backend.selectorLabels" . | nindent 4 }}
